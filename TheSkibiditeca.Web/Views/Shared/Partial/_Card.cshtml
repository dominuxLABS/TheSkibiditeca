@using TheSkibiditeca.Web.Models.Partial
@model CardModel

<div class="card bg-black-custom text-white" style="width: 19rem; height: 85%" >
    <img src="@Model.Image" class="card-img-top rounded" alt="card" style="height: 50%">
    <div class="card-body">
        <h5 class="card-title">@Model.Title</h5>
        <p class="card-text">@Model.Description</p>
    </div>
    <div class="card-footer">
        <a href="#" class="btn btn-custom-1">Ver</a>
    </div>
</div>
